.claude/

# Local secrets - never commit Google/AI credentials.
secrets/
*.service-account.json
*service-account*.json
documentai-service-account*.json*
.env
.env.*

# Local Codex/Claude working artifacts.
AGENTS.md
rc*_diff.txt
rc*_scope.txt
tools/.cache/

# Local working dirs and dev artifacts (per Codex handoff — do not commit).
PolicyLens/
backups/
test/
drive-auth-dev.ts
